# Quorum frontend notes

This directory is a self-contained Next.js app. Keep dependencies, config, and source here.

The demo is client-side until `USE_MOCK_ANALYSIS` in `lib/api.ts` is set to `false`. Full contract and wiring steps: `../../docs/FRONTEND.md`. Working notes for a later agent: `AGENTS.md` in this folder.
